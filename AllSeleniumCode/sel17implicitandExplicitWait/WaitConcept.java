package sel17implicitandExplicitWait;

public class WaitConcept {

	public static void main(String[] args) {
		/*
		 Wait: means synchronization
		 Synchrinization between script and application
		 -wait for Webelements
		 -wait for non-webelements->alerts,title of the page ,url 
		 
		 Types of wait:
		 1.static wait->Thread.sleep(5000) If element appear in 2sec it will still wait for 5sec thats it is static wait->inbulit java wait
		 2.Dynamic wait->Implicitly and explicitly wait ->selenium wait
		 
		 Explicitly Wait:
		 1.WebDriverWait
		 2.FluentWait
		 
		 WebDriver hierarchy of wait:
		 WebDriverWait(c)->FluentWait(c)->Wait(I) [until()]
		 */

	}

}
